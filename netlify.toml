[build]
  base = "apps/web-dashboard"
  command = "npm install && npm run build"
  publish = "dist"

# SPA fallback: redirect all routes to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
